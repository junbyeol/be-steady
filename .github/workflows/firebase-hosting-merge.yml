# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on Merge
on:
  push:
    branches:
      - main
    paths:
      - 'be-steady-web/**'
      - '.github/workflows/firebase-hosting-pull-request.yml'
    workflow_dispatch:
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Dependencies
        run: npm ci
        working-directory: be-steady-web
      - name: Build
        run: npm run build
        working-directory: be-steady-web
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BE_STEADY_EF28A }}
          projectId: be-steady-ef28a
          channelId: live
          entryPoint: be-steady-web
